---
description: 已拆分为 01（缓存 Core）与 02（栈占位 Adapter）；本文件仅作入口说明，不参与 always-on 约束
alwaysApply: false
---

# Figma Local Cache First（入口）

本仓库的 Figma 缓存与 Agent 约定已拆分为：

1. **`.cursor/rules/01-figma-cache-core.mdc`** — 链接标准化、本地索引、MCP、**`fc:mcp:ingest` 默认落盘**、validate；**不写业务前端**。
2. **`.cursor/rules/02-figma-<栈>-adapter.mdc`**（如 `02-figma-vue3-vite-tailwind-adapter.mdc`）— 栈专属 UI 约定；**勿**保留 `02-figma-stack-adapter.mdc` 占位（见 `project-setup finish`）。

请以上述规则为准；本文件保留作入口说明与人工查阅。
