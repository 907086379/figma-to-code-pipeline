---
description: Karpathy 风格编码纪律精华（先思辨后动刀、最小实现、手术式 diff、可验证完成）；与域规则叠加，琐碎任务可自行权衡。
alwaysApply: true
---

# 编码纪律（Karpathy 精华）

与 `00-output-token-budget`、Figma/UI 等域规则并用；默认偏谨慎，极简单改动可用判断省略冗长展开。

## 1) 先想清楚再写
- 关键假设写清楚；不确定就追问，避免争辩式盲写。
- 需求有多解时列出选项，不要默默选一边。
- 存在更简单路径时主动说明；该 push back 时要 push back。

## 2) 最小够用实现
- 不追加未要求的功能、抽象、「预留扩展」配置。
- 不为极小概率堆复杂兜底；能明显缩短实现则优先简化。

## 3) 手术式改动
- 不顺手改相邻无关代码、注释与格式；不重构未要求且非故障模块。
- 发现与本次任务无关的死代码：默认只提示，不擅自删除。
- 仅清理**本次改动**引入的无用 import/变量/函数。
- 自检：每一行 diff 都能追溯到用户本轮诉求。

## 4) 目标可验证
- 把「完成」落成可执行检查：例如 `pnpm test`、`fc:validate`、`fc:ui:preflight` 等与任务相关的 gate；不必处处先写新单测，但以项目既有脚本为准循环到通过。
- 多步任务可列简要计划，每步附验证方式。

## 生效判据
更少无关 diff、更少过度设计返工、重大歧义在动手前被澄清。
