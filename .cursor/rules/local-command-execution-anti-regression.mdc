---
description: 命令执行防回归：参数/引号/编码失败需沉淀案例（PowerShell 7 优先，兼容 5.1）
alwaysApply: true
---

# Command Execution Anti-Regression（命令执行防回归）

当脚本或终端命令因 shell 参数、引号、转义、换行、编码或跨 shell 差异失败时，必须按本规则处理，避免重复踩坑。

## 触发条件
- 命令逻辑本身正确，但在当前 shell 执行失败。
- 典型信号：参数解析错误、引号闭合错误、`node -e`/`pwsh -Command` 文本被截断或提前展开。

## 强制策略（PowerShell）
- 默认以 `pwsh`（7+）为执行目标，同时考虑 5.1 兼容风险。
- 复杂脚本（多引号/正则/JSON/多行）禁止直接 `node -e "..."`；优先顺序：
  1. 写临时 `.js` 后执行 `node <temp-file>`；
  2. here-string + stdin（`node -`）；
  3. PowerShell/.NET 文件 API 直接处理。
- `pwsh -Command` 默认使用单引号包裹命令体，避免外层 PowerShell 预展开变量。
- 需跨 5.1 稳定时，链式命令优先 `;`；写文件统一 UTF-8 无 BOM（建议 `.NET UTF8Encoding($false)`）。

## 失败后自动沉淀（强制）
- 任何命中本规则的失败，修复后必须追加到“已知故障案例（持续追加）”。
- 记录格式固定：`日期 | 场景 | 根因 | 失败写法 | 安全替代 | 验证结果`。
- 新案例只能追加，不得覆盖旧案例。

## 输出要求
- 当本轮触发此类失败，最终回复需明确：
  - 是否已按本规则完成替代执行；
  - 是否已追加案例记录；
  - 当前采用的长期安全写法。

## 已知故障案例（持续追加）
- 2026-04-15 | PowerShell 执行文本替换 | `node -e` 字符串在传参时被 PowerShell 转义破坏 | `node -e "...复杂替换逻辑..."` | 改为 here-string + `node -` 或写临时 `.js` 文件执行 | 替换脚本可稳定运行且目标文件回读正确

- 2026-04-15 | PowerShell here-string 批量替换 README/JSON | here-string 起始行后直接跟正文导致解析失败 | `$text = @'...`（头部写法不规范） | 改为临时 `.js` 文件后执行 `node <temp-file>` | 批量替换稳定执行且回读正确

- 2026-04-15 | PowerShell 生成脚本模板落盘 | 多层字符串转义导致正则/换行在目标 JS 中失真 | 通过中间模板字符串写入复杂 JS 内容 | 改为 here-string + UTF-8 无 BOM 整段重写目标文件并立即回读 | 修复后脚本可执行并正确生成 mobile-spec.json

- 2026-04-15 | PowerShell 执行临时 Node 脚本生成文档 | 模板字符串内包含 Markdown 代码围栏（```）导致 JS 字符串提前闭合 | 在 .js 模板字符串中直接内嵌 ```bash 代码块 | 改为字符串数组逐行 join 写入，继续使用临时 .js 文件执行 | 发版文件更新成功，回读与校验通过

- 2026-04-15 | PowerShell 直接写 Markdown 含反引号路径 | 反引号在双引号字符串中被当作转义，写入了异常控制字符 | 在双引号字符串里直接写 `figma-cache/docs/*.md` 行 | 改为单引号字符串或 here-string 写入，再按关键词二次回读校验 | README 路径行已修复，编码检查通过

- 2026-04-16 | PowerShell 链式命令与脚本文件写入 | 在 Windows PowerShell 5.1 中使用 `&&` 解析失败，且 `Set-Content -Encoding utf8` 产 BOM 导致 Node shebang/JSON 解析报错 | `ls "..." && ls "..."` 与 `Set-Content -Encoding utf8` 直接写 `.js/.json` | 跨环境命令改用 `;`；脚本与 JSON 统一改用 `.NET UTF8Encoding(false)` 无 BOM 写入 | `npm run verify:cursor:sync` 与 `npm run verify:cursor` 均通过

- 2026-04-16 | PowerShell 执行复杂字符串替换命令 | 直接在 shell 命令中内嵌多层反引号/模板字符串导致解析失败 | 在 PowerShell 命令参数中直接拼接复杂 `Replace(...)` 字面量 | 改为临时 `.js` 脚本执行替换并立即回读验证，再删除临时文件 | `figma-cache.js`、文档与测试更新成功，`npm test` 通过

- 2026-04-16 | 临时 Node 脚本做代码替换 | 在替换字符串中误写 `\\n` 字面量导致目标 JS 源码出现非法转义并触发 SyntaxError | 直接字符串替换注入多行 JS 代码片段 | 改为整段模板重写或替换后立即运行目标脚本校验；发现异常立即回滚并精确替换 | 修复后 `npm run verify:cursor:sync/check` 与 `npm test` 全通过

- 2026-04-16 | PowerShell 调用 pwsh -Command 变量被宿主提前展开 | 在 Windows PowerShell 中使用双引号包裹 `pwsh -Command`，`$PSVersionTable` 被外层 shell 先展开导致语法错误 | `pwsh -Command "$PSVersionTable.PSVersion.ToString()"` | 统一改为单引号包裹命令文本或使用脚本文件（如 `pwsh -Command '...'`） | `pwsh` 版本与 profile 校验命令稳定通过