---
description: Git 提交信息必须中文（工具链构造期统一规范）
alwaysApply: true
---

# Commit Message 规范（强制中文）

本规则用于工具链构造期统一提交信息风格，降低协作沟通成本。

## 1) 强制要求
- 所有 `git commit` 的提交信息（subject 与 body）必须使用**中文简体**。
- 禁止纯英文提交信息（例如 `feat: ...`、`fix: ...` 这种）。

## 2) 推荐格式
- **标题（第一行）**：一句话中文，说明“做了什么 + 为什么”
- **正文（可选）**：用 1-3 条要点说明关键影响、验证方式、风险/回滚

示例：
- `增加 batch-add 脚本并规范默认命名`
- `修复 React 挂载注入导致的 TSX 扩展名导入问题`

## 3) 例外（仍建议中文）
- 只有当提交信息必须与外部系统严格对齐（例如 release tag/自动生成），才允许夹带少量英文关键字；
  但标题仍应以中文为主。

## 4) 执行约束
- 当你准备提交时，先检查 `git status` 与 `git diff --staged`，确保没有误提交缓存/产物文件。
- 若需要分批提交，按“同一目的/同一层级”的原则拆分 commit。

